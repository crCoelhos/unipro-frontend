import React from 'react';
import PropTypes from 'prop-types';
import styles from './EventsTicketContainer.module.css';
import { Container } from 'react-bootstrap';

const EventsTicketContainer: React.FC = () => (
  <div className={styles.EventsTicketContainer}>
    <Container>
      
    </Container>
  </div>
);

EventsTicketContainer.propTypes = {};

EventsTicketContainer.defaultProps = {};

export default EventsTicketContainer;
