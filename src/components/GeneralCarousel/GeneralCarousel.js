import React from 'react';
import PropTypes from 'prop-types';
import styles from './GeneralCarousel.module.css';

const GeneralCarousel = () => (
  <div className={styles.GeneralCarousel}>
    GeneralCarousel Component
  </div>
);

GeneralCarousel.propTypes = {};

GeneralCarousel.defaultProps = {};

export default GeneralCarousel;
