import React from 'react';
import PropTypes from 'prop-types';
import styles from './AdminEventSettings.module.css';

const AdminEventSettings = () => (
  <div className={styles.AdminEventSettings}>
    AdminEventSettings Component
  </div>
);

AdminEventSettings.propTypes = {};

AdminEventSettings.defaultProps = {};

export default AdminEventSettings;
